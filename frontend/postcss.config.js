// PostCSS plugins used by Tailwind.
// `tailwindcss` turns Tailwind directives into real CSS.
// `autoprefixer` adds browser vendor prefixes automatically.

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
